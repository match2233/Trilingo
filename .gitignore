# ── 密钥：绝对不能提交 ─────────────────────────────
# 里面是明文 API key，一旦推上去几分钟内就会被爬走盗刷
data/llm.json

# ── 个人学习数据 ──────────────────────────────────
trilingo.db
trilingo.db-wal
trilingo.db-shm
trilingo.log
*.log

# 除下面两个示例词表外，其余表格一律不提交（避免误传私人表格）
*.xlsx
!词汇英.xlsx
!词汇日.xlsx

# ── 私人笔记 ──────────────────────────────────────
单词应用开发.txt

# ── 图标原图 ──────────────────────────────────────
# 只发布由它转出的 trilingo.ico，原图不入库
微信图片_*.jpg

# ── 授权原因不发布的第三方数据 ────────────────────
# 补录表：来源无许可证声明、含商业词典衍生数据，仅限个人自用
data/ja_accent.json

# Kanjium 声调库：CC BY-SA 4.0，可以发布，但 3.2MB 放仓库里不合适，
# 改由 fetch_data.py 按需下载
data/kanjium_accents.txt

# ── Python ───────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
